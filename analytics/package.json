{
  "name": "mirai-shigoto-analytics-setup",
  "version": "0.0.1",
  "private": true,
  "description": "One-shot GA4 property setup script. Reads spec.yaml, applies via Admin API.",
  "type": "module",
  "scripts": {
    "oauth-init": "node oauth-init.mjs",
    "discover": "node setup-ga4.mjs --discover",
    "setup": "node setup-ga4.mjs",
    "setup:check": "node setup-ga4.mjs --check",
    "setup:dry": "node setup-ga4.mjs --dry-run",
    "test": "node --test ga4-spec-validation.test.mjs"
  },
  "dependencies": {
    "googleapis": "^173",
    "js-yaml": "^5"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.9.0"
}
