---
/**
 * index.astro — Astro wrapper around the verbatim source HTML at
 * src/index-source.html. The page-binding logic (fs read, regex patches,
 * timestamp injection, CSS inflation) lives in _index-bindings.ts.
 *
 * Phase D audit #8 (2026-05-14): slimmed per doc §2.5.
 */
import { buildIndexPageHtml } from './_index-bindings';

const html = buildIndexPageHtml();
---
<Fragment set:html={html} />
